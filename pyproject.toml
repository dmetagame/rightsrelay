[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "rightsrelay"
version = "0.1.0"
description = "Deterministic, memory-backed release control for commercial music assets"
readme = "README.md"
requires-python = ">=3.11,<3.13"
license = "MIT"
authors = [{ name = "RightsRelay contributors" }]
dependencies = [
  "pydantic>=2.13,<3",
  "sibyl-memory-client>=0.8,<0.9",
  "x402[evm,fastapi,httpx]>=2.21,<3",
]

[project.optional-dependencies]
dev = ["pytest>=9,<10"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]

[tool.setuptools.package-data]
rightsrelay = ["console.html"]
